class Solution {
public:
    bool uniformArray(vector<int>& nums1) {

        // An array containing only even or only odd numbers is already uniform
        // If both even and odd numbers are present, every even number
        // can be made odd by subtracting an odd number

        return true;
    }
};
